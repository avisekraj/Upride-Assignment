// lib/fonts.ts
import { Anybody, Arimo } from 'next/font/google';

export const anybody = Anybody({
  subsets: ['latin'],
  weight: ['400', '600'],
});

export const arimo = Arimo({
  subsets: ['latin'],
  weight: ['400', '600'],
});
